# Логика оформления заказа для кафе Cholpon

## 📋 Обзор

Логика оформления заказа реализована через модульную систему функций, каждая из которых отвечает за свою часть процесса.

## 🔧 Основные функции

### 1. `createOrderText(name, phone, comment)`
**Назначение:** Формирует аккуратный текст заказа для WhatsApp

**Параметры:**
- `name` - Имя клиента
- `phone` - Телефон клиента  
- `comment` - Комментарий к заказу (необязательно)

**Возвращает:** Отформатированный текст заказа

**Формат текста:**
```
🍽 Новый заказ из кафе Cholpon

Пицца x2 — 900 сом
Бургер x1 — 300 сом

💰 Итого: 1200 сом
👤 Клиент: Имя
📞 Телефон: 0700xxxxxx
📝 Комментарий: (если есть)
```

**Особенности:**
- Проверяет, что корзина не пуста
- Автоматически вычисляет общую сумму
- Поддерживает русский и кыргызский языки

---

### 2. `sendToWhatsApp(orderText)`
**Назначение:** Отправляет заказ в WhatsApp

**Параметры:**
- `orderText` - Текст заказа

**Особенности:**
- Использует ссылку `https://wa.me/`
- Номер телефона кафе хранится в константе `CAFE_PHONE_NUMBER`
- Текст кодируется через `encodeURIComponent`
- Открывается в новой вкладке (`_blank`)

---

### 3. `sendToExcel(name, phone, comment)`
**Назначение:** Отправляет заказ в Google Sheets через Google Apps Script

**Параметры:**
- `name` - Имя клиента
- `phone` - Телефон клиента
- `comment` - Комментарий к заказу

**Возвращает:** Promise

**Отправляемые данные:**
```javascript
{
    date: "01.01.2024, 12:00:00",
    order: "Пицца × 2, Бургер × 1",
    total: 1200,
    customerName: "Имя",
    customerPhone: "0700xxxxxx",
    comment: "Комментарий"
}
```

**Особенности:**
- Использует режим `no-cors` для обхода CORS ограничений
- Если Google Sheets не настроен, функция просто пропускает отправку
- Ошибки не блокируют процесс заказа
- Работает параллельно с отправкой в WhatsApp

---

### 4. `placeOrder(e)`
**Назначение:** Основная функция оформления заказа

**Параметры:**
- `e` - Событие формы (Event)

**Процесс выполнения:**

1. **Валидация корзины**
   - Проверяет, что корзина не пуста
   - Показывает сообщение, если корзина пуста

2. **Валидация формы**
   - Проверяет обязательные поля (имя, телефон)
   - Проверяет формат телефона (базовая проверка)

3. **Формирование заказа**
   - Создает текст заказа через `createOrderText()`

4. **Отправка заказа**
   - Открывает WhatsApp через `sendToWhatsApp()`
   - Параллельно отправляет в Google Sheets через `sendToExcel()`

5. **Очистка**
   - Очищает корзину через `clearCart()`
   - Закрывает модальное окно
   - Показывает сообщение об успехе

---

### 5. `clearCart()`
**Назначение:** Очищает корзину и localStorage

**Действия:**
- Очищает массив `cart`
- Удаляет данные из `localStorage`
- Обновляет UI корзины
- Перерисовывает меню

---

## 🔒 Защита от ошибок

### Защита от пустой корзины
- Проверка в `placeOrder()` перед оформлением
- Проверка в `createOrderText()` с выбрасыванием ошибки
- Проверка в кнопке "Оформить заказ"

### Валидация данных
- Проверка обязательных полей (имя, телефон)
- Базовая проверка формата телефона
- Обработка ошибок через try-catch

### Обработка ошибок Google Sheets
- Если Google Sheets не настроен - заказ всё равно отправляется в WhatsApp
- Ошибки отправки в Google Sheets не блокируют процесс
- Ошибки логируются в консоль

---

## ⚙️ Константы

### `CAFE_PHONE_NUMBER`
Номер телефона кафе для WhatsApp (без + и пробелов)
```javascript
const CAFE_PHONE_NUMBER = '996500350565';
```

### `GOOGLE_SHEETS_URL`
URL Google Apps Script Web App для сохранения заказов
```javascript
const GOOGLE_SHEETS_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
```

**Важно:** Замените на ваш URL после настройки Google Apps Script (см. `GOOGLE_SHEETS_SETUP.md`)

---

## 📱 Параллельная отправка

Заказ отправляется **параллельно** в WhatsApp и Google Sheets:

```javascript
// WhatsApp открывается сразу
sendToWhatsApp(orderText);

// Google Sheets отправляется в фоне (не блокирует)
sendToExcel(name, phone, comment).catch(error => {
    console.error('Ошибка при отправке в Google Sheets:', error);
});
```

Это означает:
- WhatsApp открывается мгновенно
- Google Sheets отправляется в фоне
- Если Google Sheets не работает, WhatsApp всё равно открывается
- Пользователь не ждёт завершения отправки в Google Sheets

---

## 🎯 Пример использования

```javascript
// При отправке формы заказа
document.getElementById('orderForm').addEventListener('submit', (e) => {
    placeOrder(e);
});
```

---

## 📝 Комментарии в коде

Весь код содержит подробные комментарии:
- JSDoc комментарии для всех функций
- Описание параметров и возвращаемых значений
- Пояснения логики работы
- Важные замечания

---

## ✅ Готово к использованию

Логика полностью готова к использованию:
- ✅ Все функции реализованы
- ✅ Защита от ошибок добавлена
- ✅ Параллельная отправка работает
- ✅ Код прокомментирован
- ✅ Готов к доработке

---

**Создано для кафе Cholpon** 🍽️
